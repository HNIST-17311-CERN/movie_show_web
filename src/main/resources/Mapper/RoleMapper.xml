<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.Mapper.RoleMapper">

    <resultMap id="RoleBaseMap" type="org.example.Entity.Role">
        <id property="id" column="role_id"/>
        <result property="name" column="role_name"/>
        <result property="description" column="role_desc"/>
    </resultMap>

    <resultMap id="RoleFullMap" type="org.example.Entity.RoleDetails" extends="RoleBaseMap">
        <collection property="permissions" ofType="org.example.Entity.Permission">
            <id property="id" column="perm_id"/>
            <result property="name" column="perm_name"/>
            <result property="description" column="perm_desc"/>
        </collection>
        <collection property="users" ofType="org.example.Entity.User">
            <id property="id" column="user_id"/>
            <result property="username" column="user_username"/>
            <result property="email" column="user_email"/>
        </collection>
    </resultMap>

    <select id="findRoleFullByName" resultMap="RoleFullMap">
        SELECT
            r.id   AS role_id,
            r.name AS role_name,
            r.description AS role_desc,
            p.id   AS perm_id,
            p.name AS perm_name,
            p.description AS perm_desc,
            u.id   AS user_id,
            u.username AS user_username,
            u.email AS user_email
        FROM roles r
        LEFT JOIN role_permissions rp ON r.id = rp.role_id
        LEFT JOIN permissions p ON rp.permission_id = p.id
        LEFT JOIN user_roles ur ON r.id = ur.role_id
        LEFT JOIN users u ON ur.user_id = u.id
        WHERE r.name = #{roleName}
    </select>

</mapper>
